		<div class="mainBox">
			<div class="minNav">
				<div class="minNav"><h2>常见<span>问题</span></h2></div>
			</div>
      <div class="mainAll">
        <% form_for @ask, :url => {:controller => "admin", :action => "update_ask"}, :html => {:class => 'cmsForm', :multipart => true} do |f| %>
					<div class="cmsBox" id="i6">
						<h5>常见问题</h5>
              <% @index_content = IndexContent.new
                 @index_content.content = @ask.content if !@ask.content.blank? %>
              <%=fckeditor_textarea 'index_content', 'content' , :toolbarSet => 'Simple', :width => '600px', :height => '450px' %>
						<p class="submit"><input type="submit" value="保存" /></p>
					</div>
        <% end %>        
			</div>
		</div>
